import calculate from '../logic/calculate'

describe('calculate.js testing', () => {
    test('click on the first number', () => {
            const obj = {
                total: null,
                next: null,
                operation: null,
            } 

    })
});